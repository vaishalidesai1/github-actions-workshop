# github-actions-workshops

# Levantar un servidor web, Crear un endpoint para cada operacion:
# POST -> suma, resta, multiplicacion
# Agregar prueba del endpoint a tests
# Desplegarse con Docker en el CI->CD